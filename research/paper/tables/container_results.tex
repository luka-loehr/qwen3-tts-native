\begin{table}[t]
  \centering
  \caption{Container-level footprint. Registry compressed size is reported only when digest-bound registry evidence exists.}
  \label{tab:container-results}
  \small
  \begin{tabular}{lcccc}
    \toprule
    Engine & Unpacked size (GiB) & Compressed size (GiB) & Startup to ready (s) & Server processes \\
    \midrule
    \ContainerResultRows
    \bottomrule
  \end{tabular}
\end{table}
