# Exact copy of the stock SGLang-Omni 0.1.0 VoiceDesign example config.
# The run script overrides model_path with the locally mounted, revision-pinned
# snapshot so no model is downloaded or resolved through a moving branch.
config_cls: Qwen3TTSPipelineConfig
model_path: Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign
relay_backend: shm
